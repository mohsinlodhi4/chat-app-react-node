The Chat app is built with React on frontend and Nodejs on backend and Socket.io for websocket.

To run the project, run the following commands:
cd client-react && npm install
npm start
cd server-node && npm install
npm start

